<% if logged_in? && current_user.admin == false %>
  <div id="follow_action">
    <% unless current_user == user %>
      <% if current_user.following?(user) %>
          <%= render 'relationships/unfollow', user: user %>
      <% else %>
          <%= render 'relationships/follow', user: user %>
      <% end %>
    <% end %>
  </div>
<% end %>