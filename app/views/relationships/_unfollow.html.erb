<%= form_with(model: current_user.relationships.find_by(follow_id: user.id), method: :delete ) do |f| %>
  <%= f.hidden_field :follow_id, :value => user.id %>
  <%= f.submit 'フォロー中', class: "btn btn-unfollow text-sm", id:"un_follow_id_#{user.id}" %>
<% end %>
