describe('Users', () => {
  it('新規登録画面で新規登録を行う', () => {
  });

  it('ログイン画面から新規登録画面に遷移し、新規登録する', () => {
  });

  it('ユーザー情報の編集に成功する', () => {
  });

  it('ログインしてない場合ユーザーの編集ページにアクセスできません', () => {
  });

  it('ログインしているユーザーが違うユーザーの編集ページにアクセスできない', () => {
  });

  it('ユーザーが退会する', () => {
  });

  it('ログインしてフォローする', () => {
  });

  it('ユーザーを探すページが表示される', () => {
  });
});
