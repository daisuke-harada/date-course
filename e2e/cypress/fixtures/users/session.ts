import { testUser } from "./response";

export const testUserInput = {
  name: testUser.name,
  email: testUser.email,
  password: testUser.name,
  passwordConfirmation: testUser.name,
  gender: testUser.gender,
}